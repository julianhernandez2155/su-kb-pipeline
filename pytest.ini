[pytest]
# OneDrive's sync agent occasionally locks files in the default Windows temp
# directory mid-test, causing flaky failures. Pin pytest's tmp + cache to
# workspace-local dirs that OneDrive doesn't watch as aggressively.
cache_dir = .pytest_cache
tmp_path_retention_count = 1
tmp_path_retention_policy = failed
addopts = -ra
