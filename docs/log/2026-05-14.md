# 2026-05-14 — GitHub publish + heavy restructure

**Context**: Prep the prototype for a private GitHub repo Shahaan can read. Four conceptual layers were tangled under one flat package name (`kb_ingest`); the repo folder name implied "just conversion." Needed a coherent name + structure before any external onboarding.

**Done**:
- Folder rename: `confluence-to-md-v2/` → `su-kb-pipeline/` (had to defer to end of session — OneDrive + VSCode + Google Cloud Code extension all held file handles)
- src/ layout: moved 14 files into `src/sukb/{ingest,chat,web}/` subpackages
- Package rename: `kb_ingest` → `sukb` (import), `su-kb-pipeline` (distribution name in `pyproject.toml`)
- ~20 import rewrites across src + tests + scripts
- Tests split into `tests/ingest/` and `tests/chat/`
- `.env` copy to project root (kept parent `SU_AI_Intern/.env` unconditionally — sibling prototype still depends on it)
- `pyproject.toml` created so `pip install -e .` works
- `.gitignore` rewritten — output/raw, wiki, attachments, conversion-failures committed per intent; query-sessions gitignored with `.gitkeep`; runtime junk (job.json, response.json) deleted
- README rewritten: PowerShell-first setup, four-layer architecture, cost discipline section
- HANDOFF.md written (Shahaan-facing)
- 6 research docs copied into `docs/`: next-phase-plan-v2.md, v1-tool-brief.md, wiki-operating-model.md, eval-baseline-2026-05-13.md, wiki-proposals-2026-05-13.md, eval-queries.yaml
- Verified before commit: 110/110 tests still green, server boots cleanly, `/api/auth/status` returns `env_path_exists: true`
- `git init -b main` + `core.longpaths=true` (Windows MAX_PATH error on the deep ITSAI ancestor path)
- First commit (`faf2b9e`): 163 files, 14,882 insertions
- `gh repo create su-kb-pipeline --private --source=. --push` — live at https://github.com/julianhernandez2155/su-kb-pipeline
- Shahaan added as collaborator (read-only)

**Decisions**: [ADR-0005](../decisions/0005-src-layout-and-sukb-package-rename.md) captures the rename + restructure rationale.

**Next**: Step 6 — ceiling eval (raw+wiki mode). One command, then write up the diff. See [STATUS.md](../STATUS.md).
